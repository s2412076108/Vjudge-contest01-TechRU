#include <bits/stdc++.h>
using namespace std;

int main(){
    string str;
    getline(cin, str);
    set<char> s;

    for(int i = 0; i < str.size(); i++){
        char c = str[i];
        if(c >= 'a' && c <= 'z'){
            s.insert(c);
        }
    }

    cout << s.size() << endl;
}
